services:
  - name: street-fighter
    type: web
    runtime: static
    buildCommand: pip install pygbag && python -m pygbag --ume_block 0 --build main.py
    staticPublishPath: ./build/web
    envVars:
      - key: PYTHON_VERSION
        value: 3.12.0
